# @package _global_
name: view_resnet_subtract
notes: "test ResNet-Subtract on the view setting."

defaults:
  - override /dataset: view
  - override /model: resnet_subtract
  - override /criterion: view
