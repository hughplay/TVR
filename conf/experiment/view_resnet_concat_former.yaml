# @package _global_
name: view_resnet_concat_former
notes: "test ResNet-Concat-Former on the view setting."

defaults:
  - override /dataset: view
  - override /model: resnet_concat_former
  - override /criterion: view
