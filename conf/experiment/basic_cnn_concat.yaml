# @package _global_
name: basic_cnn_concat
notes: "test CNN-Concat on the basic setting."

defaults:
  - override /dataset: basic
  - override /model: single_cnn_concat
  - override /criterion: basic
